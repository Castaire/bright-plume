---

interface Props {
    name:           string;
    description:    string;
    size1:          string;
    price1:         number;
    size2:          string;
    price2:         number;
}

const {name, description, size1, price1, size2, price2} = Astro.props;

---